.PHONY: all opencv

all: $(SHLIB)

$(SHLIB): opencv

opencv: ; if [ ${R_ARCH_BIN} = "/x64" ]; then cd opencv-4.0.1/build64 && make install && cd ../ && rm -rf build64 && cd ../ ; fi && if [ ${R_ARCH_BIN} = "/i386" ]; then cd opencv-4.0.1/build32 && make install && cd ../ && rm -rf build32 && cd ../ ; fi && rm -rf opencv-*