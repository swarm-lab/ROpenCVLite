.PHONY: all opencv

all: $(SHLIB)

$(SHLIB): opencv

opencv: ; ls && if [ ${R_ARCH_BIN} = "/x64" ]; then cd opencv-4.0.1/build64; fi && if [ ${R_ARCH_BIN} = "/i386" ]; then cd opencv-4.0.1/build32 ; fi && make -j4 && make install && cd ../ && rm -rf build64 && rm -rf build32 && cd ../ && rm -rf opencv-4.0.1/samples ;